/*
1. Eliminar todo el subárbol que se genera a partir de un nodo n. Debe hacer un recorrido
por niveles. Se sugiere que en lugar de usar un modelo Cola, use un modelo Lista
Indexada. Recuerde que el operador básico de borrado requiere que el nodo sea una hoja.
*/

void deleteSubTree(node N) {
		
	
	
	


}

